# Draft Claude AFK Plan (DRAFT — not a commitment to build anything)

Status: not started — depends on the research and analysis files above.
Do not treat anything here as decided; this is only useful once grounded in
the actual investigation.

## To produce once evidence is in

- What should potentially be reused (as-is, from either third-party project
  or from native Claude Code mechanisms).
- What should potentially be adapted (e.g. a trimmed permission/deny-rule
  set, a lighter state-file convention).
- What should probably be left alone (functionality that exists but doesn't
  clearly pay for its complexity for a hobbyist-scale project).
- What problems remain unsolved by everything investigated.
- What the smallest sensible V1 might look like, if one is warranted at all
  — explicitly allowed outcome: "native Claude Code + a small state-file
  convention + a `RemoteTrigger` routine is already enough, build nothing."
