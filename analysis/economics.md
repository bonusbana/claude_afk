# Economics

Status: not started.

For each approach (native Claude Code, autonomous-loop, overnight-protocol),
identify where it:

- consumes additional Claude usage
- saves usage
- potentially wastes usage
- trades more tokens for higher reliability (and whether that trade is
  actually worth it, vs. orchestration overhead that doesn't materially
  improve the final result)
